function sum(a, b) {
      return a + b;
    }

    sum(1, "2");

//result is "12" due to type coersion

var a = [];
a[0] = 'test';
a[3] = 9;
alert( a );

//result is Array: ['test', , , 9] as the rest of the places in the array have been left undefined